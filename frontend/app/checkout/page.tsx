"use client";
import { useState } from "react";
const API = process.env.NEXT_PUBLIC_API_URL || "http://localhost:8000";
export default function CheckoutPage() { const [input, setInput] = useState("Show me laptops under 50000"); const [messages, setMessages] = useState<string[]>(["Hi. Tell me what you are looking for and I will prepare a test checkout."]); const [busy, setBusy] = useState(false);
  const send = async () => { if (!input.trim()) return; setBusy(true); setMessages(current => [...current, `You: ${input}`]); const response = await fetch(`${API}/api/agent/chat`, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ message: input, agent_id: "chat-demo" }) }); const data = await response.json(); const intent = data.intent || {}; const upsell = data.upsells?.[0]; setMessages(current => [...current, `${data.message} Parsed intent: ${intent.category || "any category"}${intent.max_price ? `, up to ₹${intent.max_price / 100}` : ""}.${upsell ? ` Suggested add-on: ${upsell.name}.` : ""}`]); setInput(""); setBusy(false); };
  return <main className="mx-auto max-w-4xl px-6 py-12 md:px-10"><a href="/" className="text-sm text-clay">← AM/OS</a><div className="mt-20 max-w-2xl"><p className="text-xs uppercase tracking-[0.2em] text-clay">AI buyer checkout</p><h1 className="mt-4 text-6xl leading-none">A better way to buy.</h1><p className="mt-5 text-lg text-ink/65">This chat uses the agent API and creates Razorpay Test Mode orders within the buyer spending guardrail.</p><div className="mt-12 space-y-4 border-y border-ink/15 py-6">{messages.map((message, index) => <p key={index} className={message.startsWith("You:") ? "ml-8 text-right text-ink/55" : "max-w-xl text-lg leading-7"}>{message}</p>)}</div><div className="mt-6 flex gap-3"><input value={input} onChange={event => setInput(event.target.value)} onKeyDown={event => { if (event.key === "Enter") send(); }} className="min-w-0 flex-1 border-b border-ink/35 bg-transparent px-1 py-3 outline-none focus:border-clay" placeholder="Ask for a product..." /><button disabled={busy} onClick={send} className="bg-ink px-5 py-3 text-paper disabled:opacity-50">{busy ? "Thinking" : "Send"}</button></div></div></main>; }
